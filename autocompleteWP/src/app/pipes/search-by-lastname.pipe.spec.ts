import { SearchByLastnamePipe } from './search-by-lastname.pipe';

describe('SearchByLastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByLastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
