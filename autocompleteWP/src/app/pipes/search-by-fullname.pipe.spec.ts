import { SearchByFullnamePipe } from './search-by-fullname.pipe';

describe('SearchByFullnamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByFullnamePipe();
    expect(pipe).toBeTruthy();
  });
});
